function getStyleClass(index) {
    if (index === 0) {
        return 'aa';
    }else if (index === 1) {
        return 'bb';
    }else if (index ===2) {
        return 'cc';
    }else {
        return 'dd';
    }
}

module.exports = {
    getStyleClass: getStyleClass
}